#include <stdio.h>

/**
 *main - print the 16 binary numbers in lowercase
 *Return: 0 if success
 */

int main(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	for (i = 'a'; i < 'g'; i++)
	{
		putchar(i);
	}
		putchar(10);
		return (10);
}
